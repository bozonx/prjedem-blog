# Blog of project Edem

## Run

Use node version: 18 or greater

- Install dependencies:

  ```bash
  yarn
  ```

- Run locally:

  ```bash
  yarn dev
  ```

- Build and preview:

  ```bash
  yarn build && yarn preview
  ```
