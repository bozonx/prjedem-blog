# Blog of project Edem

## Run

Use node version: 18

- Install dependencies:

  ```bash
  yarn
  ```

- Run locally:

  ```bash
  yarn dev
  ```

- Build:

  ```bash
  yarn build
  yarn preview

  # for /admin run
  yarn decap
  ```

